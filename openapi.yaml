openapi: 3.0.3
info:
  title: Ordovex
  version: 0.1.0
  description: |
    Pointer only. Canonical OpenAPI for codegen lives in
    packages/openapi-core/src/{domain}.yaml (one file per domain).
  x-product-slug: ai-fin-ordovex
servers:
  - url: https://api.example.com
paths: {}
