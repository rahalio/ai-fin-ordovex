/**
 * Certification Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: certifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import type { CertificationEntity } from "@ordovex/core/certifications/models/index.js";
import { certificationsSchemas } from "@ordovex/core/certifications";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "crt";

export function createCertification(overrides: Partial<CertificationEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    certificationId: overrides.certificationId ?? `crt_${ulid().toLowerCase()}`,
    evidenceStatus: overrides.evidenceStatus ?? "pending",
    modelVersionId: overrides.modelVersionId ?? `mdl_${ulid().toLowerCase()}`,
    riskClass: overrides.riskClass ?? "low",
    status: overrides.status ?? "pending",
    strategyId: overrides.strategyId ?? `str_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return certificationsSchemas.Certification.parse(raw);
}

export function certificationBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createCertification(overrides));
}
