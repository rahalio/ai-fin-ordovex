/**
 * GetCertification handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: certifications
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getCertification } from "@ordovex/api-server/domains/certifications/handlers";
import type { CertificationsDDDDependencies } from "@ordovex/api-server/domains/certifications";
import { createCertification as createCertificationEntity } from "@ordovex/factories/certifications";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ordovex/tests/shared";
import { CERTIFICATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetCertification", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CertificationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CertificationsDDDDependencies>(
      CERTIFICATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createCertificationEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetCertification, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { certificationId: entity.id };

    await getCertification(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetCertification.execute).toHaveBeenCalledWith(expect.objectContaining({ certificationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { certificationId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetCertification, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getCertification(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
