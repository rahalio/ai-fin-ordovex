/**
 * GetIncidentPack handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incidents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getIncidentPack } from "@ordovex/api-server/domains/incidents/handlers";
import type { IncidentsDDDDependencies } from "@ordovex/api-server/domains/incidents";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@ordovex/tests/shared";
import { INCIDENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetIncidentPack", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: IncidentsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<IncidentsDDDDependencies>(
      INCIDENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetIncidentPack, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { incidentPackId: entity.id };

    await getIncidentPack(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetIncidentPack.execute).toHaveBeenCalledWith(expect.objectContaining({ incidentPackId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { incidentPackId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetIncidentPack, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getIncidentPack(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
