/**
 * ExecuteListConcentrationAlerts - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: concentration
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListConcentrationAlerts Use Case
 *
 * DDD: Application use case for list Alert.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListConcentrationAlertsInput, ListConcentrationAlertsOutput } from "../dto/alert.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ordovex/services/_shared/index.js";
import type { AlertRepository } from "../ports";

export class ExecuteListConcentrationAlerts {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly alert: AlertRepository
  ) {}

  async execute(input: ListConcentrationAlertsInput): Promise<ListConcentrationAlertsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cncId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.alert.listConcentrationAlerts({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListConcentrationAlertsOutput;
  }
}
