/**
 * ExecuteSimulateTightestWins - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: limits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSimulateTightestWins Use Case
 *
 * DDD: Application use case for get SimulateTightest.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SimulateTightestWinsInput, SimulateTightestWinsOutput } from "../dto/simulate-tightest.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ordovex/services/_shared/index.js";
import type { SimulateTightestRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteSimulateTightestWins {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly simulateTightest: SimulateTightestRepository
  ) {}

  async execute(input: SimulateTightestWinsInput): Promise<SimulateTightestWinsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.limId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.simulateTightest.simulateTightestWins({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as SimulateTightestWinsOutput;
  }
}
