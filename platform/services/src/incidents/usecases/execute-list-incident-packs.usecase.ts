/**
 * ExecuteListIncidentPacks - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incidents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListIncidentPacks Use Case
 *
 * DDD: Application use case for list Pack.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListIncidentPacksInput, ListIncidentPacksOutput } from "../dto/pack.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ordovex/services/_shared/index.js";
import type { PackRepository } from "../ports";

export class ExecuteListIncidentPacks {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly pack: PackRepository
  ) {}

  async execute(input: ListIncidentPacksInput): Promise<ListIncidentPacksOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.incId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.pack.listIncidentPacks({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListIncidentPacksOutput;
  }
}
