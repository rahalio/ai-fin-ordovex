/**
 * ExecuteGetIncidentPack - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: incidents
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetIncidentPack Use Case
 *
 * DDD: Application use case for get Pack.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetIncidentPackInput, GetIncidentPackOutput } from "../dto/pack.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ordovex/services/_shared/index.js";
import type { PackRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetIncidentPack {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly pack: PackRepository
  ) {}

  async execute(input: GetIncidentPackInput): Promise<GetIncidentPackOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.incId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).incidentPackId) {
      throw new ValidationError("incidentPackId is required");
    }
    const incidentpackId = (input as any).incidentPackId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.pack.getIncidentPack({ incidentPackId: incidentpackId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${incidentpackId}`);
    }

    return entity as GetIncidentPackOutput;
  }
}
