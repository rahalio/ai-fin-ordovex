/**
 * ExecuteAuthoriseRestart - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: kills
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAuthoriseRestart Use Case
 *
 * DDD: Application use case for get Restart.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AuthoriseRestartInput, AuthoriseRestartOutput } from "../dto/restart.dto";
import type { ExecutionContextService, IdGeneratorService } from "@ordovex/services/_shared/index.js";
import type { RestartRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAuthoriseRestart {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly restart: RestartRepository
  ) {}

  async execute(input: AuthoriseRestartInput): Promise<AuthoriseRestartOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.kilId();

    // All field names extracted from OpenAPI spec: killOrderId, notes
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).killOrderId) {
      throw new ValidationError("killOrderId is required");
    }
    const killorderId = (input as any).killOrderId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.restart.authoriseRestart({ killOrderId: killorderId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${killorderId}`);
    }

    return entity as AuthoriseRestartOutput;
  }
}
